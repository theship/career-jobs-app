# Core dependencies
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-dotenv==1.0.1
pydantic==2.10.3
pydantic-settings==2.6.1

# Authentication
pyjwt[crypto]==2.10.1
httpx==0.28.1  # For fetching JWKS
cryptography==44.0.0  # Required for RS256/ES256

# Database
supabase==2.10.0
psycopg2-binary==2.9.10
sqlalchemy==2.0.36

# File processing (Phase 2 prep)
pdfminer.six==20240706
python-multipart==0.0.19  # For file uploads

# AI/ML (Phase 2 prep)
openai==1.57.0
numpy==2.2.0  # Updated for Python 3.13 compatibility
rapidfuzz==3.10.1  # For fuzzy string matching
onnxruntime==1.20.1  # Optional: for offline NER model
# Note: spacy removed due to Python 3.13 incompatibility
# Using OpenAI function calling + fuzzy matching instead

# Utilities
pyyaml==6.0.2  # For config files
python-jose[cryptography]==3.3.0  # Additional JWT support

# Testing
pytest==8.3.4
pytest-cov==6.0.0
pytest-asyncio==0.25.0
httpx==0.28.1  # For testing API calls (same version as above)
factory-boy==3.3.2

# Development
black==24.10.0
isort==5.13.2
mypy==1.13.0