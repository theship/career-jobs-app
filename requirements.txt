# Core dependencies
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-dotenv>=1.0.0
pydantic>=2.10.0
pydantic-settings>=2.6.0

# Authentication
pyjwt[crypto]>=2.10.0
httpx>=0.27.0,<0.28  # Constrained for supabase compatibility
cryptography>=44.0.0  # Required for RS256/ES256

# Database
supabase>=2.10.0
psycopg2-binary>=2.9.0
sqlalchemy>=2.0.0

# File processing (Phase 2 prep)
pdfminer.six>=20240706
python-docx>=1.1.0
python-multipart>=0.0.19  # For file uploads

# AI/ML (Phase 2 prep)
openai>=1.57.0
numpy>=2.2.0  # Python 3.13 compatibility
rapidfuzz>=3.10.0  # For fuzzy string matching
onnxruntime>=1.20.0  # Optional: for offline NER model
# Note: spacy removed due to Python 3.13 incompatibility
# Using OpenAI function calling + fuzzy matching instead

# Phase 4: Scoring Engine
fuzzywuzzy>=0.18.0  # For skill matching
python-Levenshtein>=0.25.0  # Speed up fuzzywuzzy
wandb>=0.18.0  # Weights & Biases for experiment tracking

# Utilities
pyyaml>=6.0.0  # For config files
python-jose[cryptography]>=3.3.0  # Additional JWT support
geopy>=2.4.0  # For geocoding locations in job normalization

# Testing
pytest>=8.3.0
pytest-cov>=6.0.0
pytest-asyncio>=0.25.0
factory-boy>=3.3.0

# Development
black>=24.10.0
isort>=5.13.0
mypy>=1.13.0